# Exact-head dispatch marker after pre-blind point-family stability regression repair.
# No runtime logic. The parent tree keeps PIT reversal/shock features, identity-regularized
# pre-blind return scaling, strict unchanged magnitude skill gates, sealed 90-100 holdout,
# current VNStock financial context, raw flow cards, TA workbench, and non-overlapping labels.
