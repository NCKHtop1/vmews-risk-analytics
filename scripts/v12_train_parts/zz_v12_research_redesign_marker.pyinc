# Exact-head validation marker for the non-collapsed magnitude redesign.
# No runtime logic.  The parent tree contains the PIT reversal/shock features, identity-regularized
# pre-blind return scaling, stronger magnitude skill gates, current VNStock financial context,
# raw foreign/proprietary flow cards, TA research workbench, and non-overlapping forecast labels.
