# Exact-head dispatch marker for the V12 H5 core-regime admission final candidate.
# No runtime logic. Parent tree preserves immutable-source-only runtime, exact label-maturity OOF,
# robust L1 expert-family selection locked before 70%, reserved 70-80% architecture selection,
# maturity-purged 80-85% point fitting, independent early 85-90 validation, late 85-90 conformal
# calibration, and sealed 90-100 evaluation with zero sealed labels supplied to selection.
#
# H1-H4 retain their previously passing NUMERICAL+REGIME core path. H5 removes the accidental
# exemption that forced REGIME into the core: NUMERICAL is the H5 baseline and the existing
# _core_regime_decision helper may admit REGIME only when the reserved 70-80% block demonstrates
# positive incremental Rank IC, positive MAE improvement, non-degrading spread, one-sided HAC
# significance, and positive moving-block-bootstrap lower-90 IC evidence. Optional EVENT/FLOW/
# FUNDAMENTAL_EVENT/RUMOR admission remains unchanged and multiple-testing controlled.
#
# No authority threshold is weakened. V8-V11 point challengers remain fail-closed; this candidate
# changes architecture admission rather than calibrating against the observed sealed H5 outcome.
# Deterministic five-horizon assembly and exact Fast -> Full promotion remain authoritative.
