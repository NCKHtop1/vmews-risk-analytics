# Exact-head dispatch marker after the pre-blind monotone-median point-calibration fast gate passed.
# No runtime logic. The parent tree contains PIT reversal/shock features, identity-regularized
# pre-blind return scaling, strict unchanged magnitude skill gates, current VNStock financial
# context, raw foreign/proprietary flow cards, TA research workbench, and non-overlapping labels.
