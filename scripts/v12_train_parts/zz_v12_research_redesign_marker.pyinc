# Exact-head dispatch marker for gate-aligned V12 final candidate.
# No runtime logic. Parent tree contains immutable-source-only runtime, exact-maturity OOF with
# robust L1 family, chronology-correct no-change-aligned point selection with one-SE magnitude
# regularization, deterministic five-horizon assembly regression, sealed 90-100 evaluation,
# unchanged scientific authority gates, and observable exact Fast/Full workflow status.
