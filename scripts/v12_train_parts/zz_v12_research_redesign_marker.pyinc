# Exact-head dispatch marker for the fit-locked V12 final candidate.
# No runtime logic. Parent tree contains immutable-source-only runtime, exact-maturity OOF with
# robust L1 family, fit-locked monotone tail-rank point shape/scale selected only on 80-85%,
# scale selection requiring positive moving-origin-date block-bootstrap lower-90 MAE evidence,
# H5-only market+tail V9 and sparse asymmetric tail-median V10 as fail-closed challengers, plus
# final H5 quantile-scaled sparse-tail V11: middle ranks stay exact no-change while extreme-rank
# magnitude is sourced only from the already fit-locked q20/q80 surface. Threshold/quantile scale
# are selected on maturity-purged 80-85% only, must remain inside the pooled-MAE one-SE envelope,
# must satisfy unchanged TOTAL forecast magnitude floors and positive moving-origin-date lower-90
# MAE evidence, and may only be confirmed by independent early whole-date 85-90. Late 85-90 is
# conformal-only and 90-100 remains sealed with zero labels supplied to selection.
# Deterministic five-horizon assembly, benchmark alignment, entity/rumor/flow/sector contracts,
# venue-aware source acceptance, sparse-regime abstention, unchanged scientific authority gates,
# and observable exact Fast/Full remain authoritative.
