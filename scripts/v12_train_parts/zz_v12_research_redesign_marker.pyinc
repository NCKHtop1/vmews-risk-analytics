# Exact-head dispatch marker for the consolidated V12 final candidate.
# No runtime logic. Parent tree contains immutable-source-only runtime, exact-maturity OOF with
# robust L1 family, nested expanding-origin pre-blind point/scale CV, late-preblind conformal-only
# calibration, deterministic five-horizon assembly, factual entity/rumor audit restoration,
# venue-aware source acceptance, explicit sparse-regime abstention, sealed 90-100 evaluation,
# unchanged scientific authority gates, and observable exact Fast/Full workflow status.
